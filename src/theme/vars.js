import { Dimensions } from 'react-native';

const { height, width } = Dimensions.get('window');
export default {
    deviceHeight: height,
    deviceWidth: width,

    primaryColor: '#1A3D80',
    acentColor: '#2196F3',
    borderColorDark: '#B3B3B3',
    statusBarColor: '#486BAE',
    cointainerBackgroundColor: 'white',
    textColor: '#212121',
    infoTextColor: 'grey'
};
